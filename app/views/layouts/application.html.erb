<!DOCTYPE html>
<html lang="en">
<head>
  <title>LibraryApp</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <!-- Tailwind CSS -->
  <script src="https://cdn.tailwindcss.com"></script>

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
</head>
<body class="bg-gray-100 text-gray-900">

  <!-- Navigation Bar -->
  <nav class="bg-blue-600 text-white py-4 px-6 flex flex-col md:flex-row justify-between items-center shadow-md">
    <h1 class="text-2xl font-semibold mb-4 md:mb-0">📚 LibraryApp</h1>
    
    <% if current_user %>
      <div class="flex flex-col md:flex-row items-center space-y-2 md:space-y-0 md:space-x-4">
        <span class="font-medium">Logged in as <%= current_user.username %></span>
        <%= link_to "Profile", profile_path, class: "bg-yellow-500 hover:bg-yellow-600 text-white px-4 py-2 rounded-md" %>
        <%= button_to "Log Out", logout_path, method: :delete, class: "bg-red-500 hover:bg-red-600 text-white px-4 py-2 rounded-md" %>
      </div>
    <% else %>
      <div class="flex flex-col md:flex-row space-y-2 md:space-y-0 md:space-x-4">
        <%= link_to "Login", login_path, class: "bg-blue-500 hover:bg-blue-600 text-white px-4 py-2 rounded-md" %>
        <%= link_to "Sign Up", signup_path, class: "bg-green-500 hover:bg-green-600 text-white px-4 py-2 rounded-md" %>
      </div>
    <% end %>
  </nav>

  <!-- Flash Messages -->
  <% if notice || alert %>
    <div class="container mx-auto mt-4 px-4 md:px-0">
      <% if notice %>
        <p class="bg-green-100 text-green-800 p-3 rounded-md shadow-md"><%= notice %></p>
      <% end %>
      <% if alert %>
        <p class="bg-red-100 text-red-800 p-3 rounded-md shadow-md"><%= alert %></p>
      <% end %>
    </div>
  <% end %>

  <!-- Main Content -->
  <div class="container mx-auto p-4 md:p-6">
    <%= yield %>
  </div>

</body>
</html>